routes:
  - uri: /api/graphql
    upstream:
      nodes:
        "127.0.0.1:14202": 1
      type: roundrobin
    plugins:
      cors: {}
      openid-connect:
        # https://github.com/apache/apisix/issues/5425
        client_id: AIONBgkMNra8acZnzpDEp
        client_secret: ng3niteUBC1LXVaPgHkQt
        discovery: https://logto.pocki.cc/oidc/.well-known/openid-configuration
        use_jwks: true
        bearer_only: true
        access_token_in_authorization_header: true
        set_userinfo_header: true
  - uri: /api
    upstream:
      nodes:
        "127.0.0.1:14202": 1
      type: roundrobin
    plugins:
      cors: {}
#END
